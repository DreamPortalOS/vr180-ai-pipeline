torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
tqdm>=4.65.0
ffmpeg-python>=0.2.0

# Depth estimation (optional — pick one)
# transformers>=4.35.0      # Depth Anything V2 via HuggingFace
# accelerate>=0.24.0
# safetensors>=0.4.0

# Optical flow (optional)
# raft-stereo>=1.0.0

# Video codec
# ffmpeg (system package)
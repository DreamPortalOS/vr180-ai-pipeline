# 生成任务单：1:1 宽视角真前进素材（owner 操作）

> 依据：`docs/RESEARCH_COVERAGE_V2.md`（已合并）。目标：让 180° 视野里的**主要内容都是生成出来的**，
> 边缘只靠 165→180° 羽化收口（F-9 已默认开启），不再靠拉伸/外扩/累积补边。
> 老单 `SOURCE_SPEC.md` 已作废（静态相机 + Gemini 等距两处错误），别再照它做。

## 第 1 步：先用 draft 档试运镜（不花 4k 额度）

| 项 | 值 | 为什么 |
|---|---|---|
| 模型 | `doubao-seedance-2-0-260128`（**标准档**，不是 fast） | fast 档没有 4k / 1:1 组合；`--model` 已在 #246 解锁 |
| ratio | **`1:1`** | 方形才能把水平和垂直两个方向同时铺到 ~115–120° |
| resolution | 先 **`480p`** 试运镜；确认听话后再 **`4k`**（= 2880×2880） | 4k 输出 10bit HEVC，管线冒烟卡 C-3 正在做 |
| duration | 先 **5 s** 看运镜；合格后 10–15 s | 省额度 |
| 首帧 | **正方形**关键帧（不是 16:9！方舟会居中裁掉两侧） | 图片来源随意：Agnes / SenseNova / NVIDIA 均可，要点是方形 + 超广角构图 |

仓内命令（draft 档，一次；prompt 从 `GEN_PROMPT_1x1.txt` 读入，作为位置参数）：
```bash
python scripts/generate.py --provider seedance --model doubao-seedance-2-0-260128 --gen-ratio 1:1 --gen-resolution 480p --duration 5 --draft --image 你的方形首帧.png -o video/gen_1x1_draft.mp4 "$(cat GEN_PROMPT_1x1.txt)"
```
上 4k 时只改两处：去掉 `--draft`，`--gen-resolution 4k`。

## 第 2 步：prompt（原文，直接用）

```
一镜到底的第一人称飞行长镜头，超广角镜头（明显的透视拉伸，画面四角物体被拉宽，地平线偏低，近大远小夸张）。
镜头以恒定速度持续向前推进，从头到尾不停、不减速、不后退、不切换机位、不做任何转场。
主体与运动：一条向前延伸的峡谷通道，两侧层叠巨型岩壁与悬空植被，前方远处一个明亮开口作为消失点，镜头始终朝它飞。
岩壁、藤蔓、尘埃随镜头推进从画面中心持续向四周散开并掠出画面边缘，越靠边移动越快，形成稳定的径向外流。
运镜只有一种：持续前推，匀速，机位高度不变，不摇不摆不旋转。
光线均匀柔和；四边不要大面积纯白过曝或纯黑死区；无文字、字幕、水印、人脸；无贴脸掠过的物体；无黑边、信箱条、圆形暗角。
```

场景可以换（森林隧道 / 城市峡谷 / 海底裂谷都行），但**前四行的运镜与视角描述别改**——它们对应管线的三个硬需求：
持续前进（真视差、深度稳定）、超广角（覆盖角）、径向外流（源片边缘有内容可铺到 165°）。

## 第 3 步：自检（任一不过 → 直接重生成，别送进管线）

1. **尺寸**：`ffprobe` 看到 2880×2880（4k 档）或 480×480（draft 档）。
2. **真前进**：肉眼看画面边缘的东西是否持续向外掠出画面。静态机位的特征是"东西在原地轻微呼吸"。
   （工具化的光流检查我这边跑：把文件名告诉我即可。）
3. **视角够宽**：四角明显被拉宽、地平线偏低。若看起来像普通 50mm 镜头 → 加强"超广角/透视拉伸"措辞重生成。
4. **hfov 不信 prompt**：真实视角由校准工具（C-1，进行中）从画面里量；你不用管，把片子给我即可。

## 第 4 步：交给我

把文件放到 `video/`（不进 git），在 WORKLOG 留言区写文件名。我这边：C-1 校准 hfov → 走管线（默认羽化）→ 推 Quest → 出 `QUEST_TEST_1x1.md` 给你戴机打分。

## 管线侧已就绪（09-08 更新，都已合并进主干）

- **10bit 4k 源已实证可吃**（#298）：CI 每次都用真 `yuv420p10le` 跑完整链路。⇒ **上 4k 的技术风险为零**，
  只剩"模型听不听话"，draft 一次就能看出来。
- **鱼眼备选路线已打通**（#299）：若 1:1 平面宽视角效果不理想，可直接出圆形鱼眼素材，走
  `--input-projection fisheye --fisheye-fov 180`。
- **视场校准工具已就位**（#297）：`python scripts/calibrate_hfov.py <素材>`——注意它量的是**等距鱼眼视场**，
  只对鱼眼素材有意义（针孔源的视角在数学上无法用此法测）。

## 我这边先不动的事（等你拍板）
- **4k 档额度**：draft 看到真前进 + 够宽之后再上 4k，由你说"可以"我再指导跑。
- **C-4 探针**（黑底 1:1 首帧会不会被画满）：需要 draft 一次，你允许我就跑。

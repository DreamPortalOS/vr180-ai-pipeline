# 项目简报 · 给 owner（2026-09-09）

主干 `1d1fddf`，**2897 passed**，ruff 干净，无开放 PR。
本文档三部分：**① 等你决策/操作的事 ② 已完成的开发 ③ 接下来的开发**。

---

# ① 等你决策 / 操作

## A. 【操作】设两个环境变量，预算护栏才会生效

充值后在 PowerShell 里跑一次（之后所有新窗口都带着）：

```
[Environment]::SetEnvironmentVariable('VR180_ARK_PRICE_PER_MTOKEN','27.75','User'); [Environment]::SetEnvironmentVariable('VR180_BUDGET_CAP','100','User')
```

设完：累计到 **80 元警告**，到 **100 元在提交请求之前拦住**（拦在花钱之后没意义）。
硬上限仍需你在[方舟控制台](https://console.volcengine.com/ark)费用中心自行设，我没有你的账单权限。

**为什么急**：查证到的官方单价（限时 75 折，10/07 后回 37 元/百万 token）回算——
**4k/10s 一条约 50 元**，充 100 元只够两条。

## B. 【操作】Gemini 出图的要求（你说没看到，这是我漏给的）

之前只给了**视频** prompt，没给**关键帧图片**的要求。补在这里，同时给出验收方法。

### 出图 prompt（复制即用）

```
第一人称超广角视角，正方形画幅。

画面正中偏中央有一架深灰色四旋翼无人机在飞行，距离镜头约八米，占画面面积约 10%，
是画面唯一的视觉焦点。机身是不透明的哑光深灰色金属与碳纤维材质，表面无反光、不透明、不反射环境；
机臂笔直固定，四个旋翼高速旋转成半透明圆盘状运动模糊；机身下方有小型云台相机与红色指示灯。

环境：一条向前延伸的红岩峡谷，谷底湍急河流白色浪花翻涌，远处峡谷蜿蜒延伸，
可见远山、树林与层次分明的岩壁——远景要有清晰可见的具体景物。

【构图硬要求】细节必须集中在画面中央三分之一：中央密、周边空。
四角与画面外圈自然压暗、只保留低对比度的背景（岩壁阴影、远景虚化），不要把纹理铺满到边缘。
画面内容完整填满整个正方形画幅，四角都是实景。

不要一片纯白的天空或出口，不要大面积没有细节的亮区；
不要圆形取景、不要黑色暗角、不要鱼眼圆框、不要黑边、不要相框；
不要透明或玻璃材质、不要镜面反射、不要人物、不要动物；无文字、无水印。写实电影感。
```

### 出图后先验，再决定要不要花钱生成视频

```
.venv\Scripts\python.exe scripts\check_source_quality.py 你的图.png
```

三条硬指标（都来自 Red Raion 34 条成片 + 613 张剧照的实测）：

| 指标 | 目标 | 为什么 |
|---|---|---|
| `detail_ratio` 中央/周边细节比 | **≥1.2**（竞品 1.58–1.60） | VR 画面外圈是几何最假、羽化最明显的一圈；边缘越有细节越容易被发现 |
| `anchor` 主体面积 | **8–15%**（竞品中位 11.3%） | 太小抓不住视线，太大糊住画幅 |
| `anchor` 偏心 | **≤0.3**（竞品中位 0.236） | 主体偏了，视线会被带向边缘那圈瑕疵 |

**已知限制**：显著性算法测的是"与画面平均色的距离"。上次那张图里红棕岩石主导了平均色，
灰色无人机**没被识别成主体**，检出的是下方白色水花。所以 `anchor` 那两条读数**仅供参考**，
`detail_ratio` 是可信的。修这个限制需要额外的纹理对比通道，已列入待开发。

### 更正：Gemini 能出 1:1
我此前说"Gemini 只能出 16:9/9:16"是错的——owner 实测**只要 prompt 写得好就能直接出 1:1**
（`Gemini_v1.jpg` 实测 1024×1024）。所以**首选 1:1**，不必退而求其次用 16:9。

### 别在 prompt 上纠结的一条
"不横滚不摇摆"这类措辞**有效但有限**：实测无人机那条（写了禁止）比水晶那条稳定 **约 3 倍**
（低频 RMS 0.58° vs 1.98°），但都消不掉低频漂移。彻底解决要靠管线侧的稳定化。

## C. 【决策】球幕场馆形态

D-1 已修好 fulldome 的四处硬伤，但**再往下走必须先知道**：

- 球幕**直径与倾角**？（决定几何参数）
- **warp/blend 由谁做**——我们出母版、场馆播放系统做变形？还是我们要出已变形的成片？
- 需要**立体**吗？（球幕通常单目，偏光在曲面上失效）

不给这三个答案，D-2 之后的卡我不会开——参数全悬在上面。

**另一个只有你能拍板的**：前向 180° 的源片**永远填不满穹顶**（100% 覆盖需要视场 ≥ `2×(90°+光轴仰角)`，
仰角 30° 就要 240°）。所以要么接受留白，要么把光轴指向天顶。这个观感差别 ffplay 看不出来，
得你在真球幕或桌面鱼眼播放器上看一眼。

## D. 【决策】素材迭代的节奏

按 50 元/条，建议：**Gemini 免费出内容 → 我过管线看几何与舒适度 → 定稿了才用 Seedance 出一条 4k**。
你已经认可这个思路，写在这里备查。

---

# ② 已完成的开发（本轮 25 个 PR）

## 素材侧：三条规格已定论，都有实测支撑

| 规格 | 结论 | 证据 |
|---|---|---|
| 投影 | `--fisheye-fov 180` | 黑区 4.0%、安全转头 ±38°（140 档是 37.6%/±18°，你实测左右会看到边） |
| 锚点主体 | 必须**不透明刚体** | 水晶中心左右眼匹配误差 24.4、河面 5.5——透明让深度图穿透，这就是你说的"聚焦不起来"，与瞳距无关 |
| 环境 | 必须明确写"动" | 你反馈静止河水出戏；改后你说"河水流动非常好" |

## 管线能力（新增）

- **素材体检** `check_source_quality.py`：方图/真前进/边缘/可解码 + 静帧模式 + 中央周边细节比 + 锚点主体
- **体检接入管线** `--source-check`：strict 时在构造重模型**之前**退出（覆盖 8 个构造点，变异测试证明有效）
- **鱼眼输入** `--input-projection fisheye` + `--fisheye-fov`
- **球面朝向** `--pitch/--yaw/--roll`（旋转约定是**实测拟合**出来的，六种顺序里 ypr 拟合到 0.003）
- **运动测量** `analyze_motion.py`：合成标定 RMS 0.00032°，纯前进虚报 0.010°，带长基线交叉检查
- **视场校准** `calibrate_hfov.py`：量等距鱼眼视场（针孔源在数学上无法用此法测，已证明）
- **用量账本 + 预算软闸**
- **球幕修复**：四角纯黑 0%→100%、内容圆半径 1.414R→1.000R、成片不再静音、`iv_fov` 67.5°→88.5°

## 测试门禁：修掉三条假红，本机与 CI 现在等价

之前本机必挂一条，执行者要么被干扰、要么可能为了让它绿去改实现。

- 环境变量泄漏（本机设了 `ARK_API_KEY` 就必挂）
- 两条墙钟性能断言（32 进程负载下 10/10 全红，改成工作量断言后 10/10 全绿）
- 仓库根污染守卫：发现 **8 处**泄漏，其中一处是测试在 CI 里**真的 `git clone`**（全量测试 186s→127s）

## 我犯的两个错误（已纠正）

1. **横滚 21° 是测量假象**。我用 ORB 逐帧累积，累积偏置把 1.7° 放大成 21°。
   直接首尾配准是 −1.72°，两种前端一致到 0.03°。因此"prompt 完全没听"的结论**方向也是错的**——
   prompt 其实让它稳定了 3 倍。现在的估计器带长基线交叉检查，会主动报出这类漂移。
2. **`video/src_720p_v2.mp4` 不存在**——是我列目录被 `head -5` 截断看漏。

---

# ③ 接下来的开发

## 已派单（本轮重启后需重新派，零提交）

| 卡 | 内容 |
|---|---|
| #322 W-7 | 体检对圆形鱼眼源假阴性（圆外黑区污染统计） |
| #319 K-29 | 干掉"超时即通过"的假测试 |
| #320 K-30 | `setup_seedvr2 --dry-run` 会真建目录 |
| 调研 | **本地部署视频模型**（Hailuo 3 能否自建 + 12GB 上跑得动的开源方案） |

## 待开卡（等前置条件）

| 卡 | 前置 |
|---|---|
| 平移/俯仰测量 | 横滚已确认仅 1–2°，**不足以解释你的眩晕**；要把测量扩到另外两个自由度才能定位成因 |
| 稳定化 `--stabilize` | 等测量扩完，否则不知道该消除什么 |
| 锚点检测加纹理通道 | 修上面说的"灰色主体识别不出"的限制 |
| 分镜表 → 多场景装配 | `segment_concat.py` 已实现但**没有 CLI、没接进批处理**，5 秒片段拼成 15–25 秒场景目前要手写 Python |
| D-2 球幕 CLI 接线 | **等你的场馆形态答复** |

## 硬件现状（供本地部署调研参考）
RTX 4070 SUPER 12GB · i7-14700KF · 32GB 内存 · **D 盘剩 126GB、C 盘剩 70GB**
（我先前报的 55GB 是临时文件占满时的读数，已更正）。磁盘不再是硬约束。

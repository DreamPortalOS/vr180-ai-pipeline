# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Output / cache
output/
*.mp4
*.mkv
*.webm
*.mov
*.avi
*.png
*.jpg
*.jpeg
*.npy
*.npz
*.pt
*.pth
*.onnx
*_vr180_temp/
*_vr180_temp*/
*.pyc
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage

# Depth-product content cache (I-8a, issue #182): content-keyed .npy + meta.json
# under models/.cache/depth/<key>/. models/ is already ignored above; this line
# documents the cache subtree explicitly so the intent is obvious.
models/.cache/

# Jupyter
.ipynb_checkpoints/

# Video experiment artifacts (should not be tracked)
video/*.md
video/*.json
# Mock-provider leakage — double-gate so test/CI artifacts never re-appear
# if the provider's tempdir default is ever regressed (issue #161).
video/mock_*.mp4

# Model weights (too large for git)
models/
!models/.gitkeep

# In-repo third-party deps (SeedVR2 node + its dedicated venv — gitignored, never committed)
third_party/

# Config with local paths
.env
*.local.yaml

# Cline private scratch state (NOT the shared CLINE_TASK_BOARD.md)
.cline-state.md

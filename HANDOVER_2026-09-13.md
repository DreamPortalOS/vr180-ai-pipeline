# 🔁 交接 · 2026-09-13（接手 agent 从这份读起）

> 状态：main = `cb963d1`（已推 origin）。代码已合并，当前无进行中的后台渲染任务。

## 0. 一句话现状
VR180 本地转制管线已跑通（源片→深度→立体→投影→编码全本地、零 AI 调用）。
G-7 无人机立体修复版成片已生成并推上 Quest 3，**正在等 owner 实测反馈（Q1–Q5）**。

## 1. 已在跑 / 已交付
- ✅ **G-7 立体修复**已合并进 main（`cb963d1`）：
  - `pipeline/stereo_renderer.py`：全场 disocclusion 修复（原 `_inpaint_holes` 只修边框 5% → 物体边缘透明/空洞）；新增 `src_hfov` 参数（原来 focal 硬编码 70° → 视差过大、太近太巨大、两眼不融合）。
  - `scripts/run_pipeline.py`：新增 `--src-hfov`，透传给 renderer。
  - `pipeline/streaming_pipeline.py`：透传修复。
- ✅ **#348 anchor rescue**（`check_source_quality.py` + 测试）同批合并。
- ✅ 成片 `gen_1x1_4k_drone_vr180.mp4`：7680×3840 HEVC 24fps 10s，QA 6/6 全绿（sv3d+st3d、SBS 每眼 3840²、音频 AAC），全帧解码零错误。
- ✅ 已推 Quest 3：`/sdcard/Movies/gen_1x1_4k_drone_vr180.mp4`（67163842 字节，媒体扫描已触发）。

## 2. 正在等 owner（唯一阻塞）
按 `QUEST_TEST_DRONE_G7.md` 答 5 问（Q1 透明/空洞、Q2 大小、Q3 两眼融合、Q4 旋翼、Q5 溪水）。
收到后按文件底部"反馈对照"分流：
- Q1/Q2/Q3 仍有问题 → 继续调 stereo（同 `pipeline/stereo_renderer.py`）。
- Q5 溪水静止不可接受 → 源片生成阶段加动效约束 / 换首帧（不是管线问题，源片就是静止的）。
- 全过 → 同流程处理 crystal/plain + 开长片 CLI 卡。

## 3. 排期（详见 NEXT_DEV_PROMPTS.md，按序派单）
- G-1 接班巡检（先做）→ G-2 串行合 #352（#351 已合）→ G-3 剪枝清理 → G-4 E-4 MiniMax 验证（#353）→ G-5 D-2 球幕（等场馆方交付规范）。
- G-6 owner 待办：①发 D-2 供应商缺口清单 ②答 G-7 测试 ③设预算护栏环境变量。

## 4. 待合并 / 待派单卡
- PR #352（#322 fish-eye-forward）：需在 #351 后串行 rebase + 变异检验再合。
- Issue #353（E-4 MiniMax）`stage:ready`，等派单。
- D-2 球幕：被外部阻塞（等场馆方回交付规范）。

## 5. 环境 / 纪律要点
- 本机管线零成本（不调任何 AI 接口）。源片生成才花额度（Seedance / Gemini / Veo 等）。
- 渲染跑长任务用后台 job + 日志/pid 文件 + 轮询，勿多进程并发写同一输出（曾因两个 rebuild 互踩写出坏 moov + NAL 损坏）。
- 测试必须前台跑：`pytest -m "not slow"` 全绿 + `ruff check` 干净。
- 推 Quest 只推 `2G0YC1ZF7X006Z`（Quest 3）；`1C041FDF6004N2` 是 Pixel，**不要动**。

## 6. 已知坑（写进给后来者）
- adb push 大文件命令易报 30s 超时，实际可能已传完——用 `adb shell ls -l` 校验字节数再决定重推。
- 之前 G-7 坏片根因：两个并发渲染进程互踩输出 → 单任务跑，勿并发。

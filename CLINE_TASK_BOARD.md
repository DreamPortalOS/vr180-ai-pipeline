# ✅ P-2: Prompt Lab — CLI batch prompt variant generator

- [x] Branch: `feat/P2-prompt-lab` (cut from `feat/P1-prompt-target-aware`)
- [ ] Create `scripts/prompt_lab.py` — CLI, cross-prod targets×scenes, manifest JSON, terminal table
- [ ] Create `tests/test_prompt_lab.py` — structure + count assertions
- [ ] Run `ruff check . --fix` → green
- [ ] Run `pytest` → green
- [ ] `git add` + `git commit` + `git push` + create PR

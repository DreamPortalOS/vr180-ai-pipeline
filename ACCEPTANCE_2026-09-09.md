# 验收报告 + 测试决策清单（2026-09-09）

主干 `4088f1c`，无开放 PR，远端分支已收敛到 `main` + 归档两条。
本文档：**① 验收结论（含我亲自复核的部分）② 你要测的清单 ③ 你要决策的清单**。

---

# ① 验收结论

## 我亲自复核过的（不是听执行者说的）

| 项 | 复核方式 | 结果 |
|---|---|---|
| 主干健康 | 本机全量 `pytest -m "not slow"` | **2897 passed / ruff 干净**，且是在设着真实 `ARK_API_KEY` 的环境下跑的 |
| 球幕四处硬伤 | 直接 grep 源码 | 全部属实：`-an` 在第 111 行、零处黑底合成、`iv_fov` 用线性比例、无 pitch 支持 |
| 横滚真值 | 自己写首尾直接配准 | 无人机 +1.37°、水晶 +1.86° —— 证实我此前报的 21° 是累积假象 |
| 运动画像 | 跑 `analyze_motion.py` | 水晶峰峰 6.67°（低频占 60.6×）、无人机 2.35°（3.5×），均判为低频漂移 |
| 你的关键帧 | 跑体检 + 复现显著性 | `detail_ratio` **1.96** 合格；`anchor 偏心 0.62` 是**误报**（检出的是天空，质心 0.49/**0.06**） |
| 磁盘 | `df` + PowerShell 双查 | **D 剩 126GB、C 剩 70GB**——我此前报的 55GB 是临时文件占满时的读数 |

## 执行者报告里被我查出的错误

| 出处 | 错误 | 事实 |
|---|---|---|
| 本地视频调研 | 称 `pipeline/segment_concat.py` **在本仓不存在**（"find + grep 双重核实，全仓零命中"） | **存在**，20589 字节，`5dd4431` 提交（#199）。它用这条支撑了"选 H3 而非 Wan"的论证，结论未必错但依据是错的 |
| 我自己 | 磁盘 55GB | 实为 126GB。本地部署的磁盘约束不成立 |
| 我自己 | "Gemini 只能出 16:9" | 你实测可直接出 1:1 |

## 执行者查出的、我认可的我方错误

1. **横滚 21° 是我的测量假象**（ORB 逐帧累积偏置）。真值 1.7–1.9°，且 prompt 里的禁止措辞**是有效的**——
   写了禁止的那条比没写的稳定约 3 倍。我当时的结论连方向都反了。
2. **我的变异检验卡片写错了**：要求"去掉黑底合成则四角断言变红"，实际不会，因为圆掩码是第二道独立防线。
3. **`ANALYSIS.md` 有 3 处正文与表格自相矛盾**（平均亮度 56.5 vs 56.9、边框比 0.56 vs 0.492、四角比 0.55 vs 0.54），
   已在 `CONTENT_SPEC.md` 里逐条标注。

## 本轮合并的开发（26 个 PR）

**素材侧**：体检工具（4 项基础 + 静帧模式 + 中央/周边细节比 + 锚点主体）、接入管线的 `--source-check`
（strict 时在构造重模型**之前**退出，覆盖 8 个构造点）、鱼眼输入、球面朝向 `--pitch/--yaw/--roll`、
视场校准、运动测量、用量账本 + 预算软闸、球幕四处修复。

**测试门禁**：修掉三条假红（环境变量泄漏、两条墙钟性能断言），发现并修掉 8 处仓库根污染
——其中一处是测试在 CI 里**真的 `git clone`**（全量耗时 186s→127s）。**本机门禁现在与 CI 等价**。

---

# ② 你要测的清单

## 现在就能测（机器上已有）

| 文件 | 测什么 | 我要你回答 |
|---|---|---|
| `quest_4k_fe180.mp4` | 4k + 摊满 180°（黑区 4.0%，安全转头 ±38°） | 左右转头还看得到边吗？ |
| `quest_4k_fe140.mp4` | 4k + 摊到 140°（黑区 37.6%，±18°） | 与上面比，哪个更好？ |
| `quest_1x1_sameSrc_fe140/fe180.mp4` | 480p 同源对照 | 只在需要确认"是投影差别还是素材差别"时看 |

## 等新素材出来要测的

1. **无人机版**（刚体锚点）：会不会重现鸟的关节崩坏、水晶的透明空洞？
2. **稳定化前后对比**：等平移/俯仰测量扩完再出——横滚只有 1–2°，**不足以解释你的眩晕**，
   成因还没定位，现在做稳定化是盲修。

---

# ③ 你要决策的清单

| # | 决策 | 为什么卡在你这 | 影响 |
|---|---|---|---|
| 1 | **球幕场馆三问**：直径/倾角确认、warp+blend 由谁做、要不要立体 | 两个调研正在读你给的 PDF，但有些只能问对方 | D-2 之后所有球幕卡的参数 |
| 2 | **半球留白的观感取舍** | 前向 180° 源**永远填不满穹顶**（100% 覆盖需视场 ≥ `2×(90°+仰角)`），ffplay 看不出差别 | 球幕内容的运镜设计 |
| 3 | **本地部署投不投入** | 调研结论：MiniMax-H3 权重已开源但**缺 2K 重生成那段**；本地 768p 折算 6.5 px/deg vs Seedance 4k 的 16 px/deg | 是否值得花 63GB + 若干小时验证 |
| 4 | **是否验证 MiniMax 官方 API** | 调研查到 2K 约 $0.13/秒 ≈ **9.5 元/10 秒**，对比 Seedance 50 元；但 2K 短边 1440 够不够我们的规格**未实测** | 可能比本地部署和 Seedance 都划算 |
| 5 | **预算护栏两个环境变量** | 我没有你的账单权限 | 不设的话软闸不生效 |

## 我的建议（按优先级）

1. **先测 #4（MiniMax API）**——如果 9.5 元能出够用的画质，迭代成本直接降 5 倍，比折腾本地部署快得多。
2. **球幕三问尽快问对方**——两个调研会给你一份"需要确认的问题清单"，照着问即可。
3. **本地部署暂缓**——本地一条要 1.5–2.5 小时，并不比 Gemini 更适合快速迭代；它的价值只在"不按条计费"。
